# TAK-ADSB-Feeder Configuration v2.1
# TAK Server Integration (Hardcoded Priority)

# ============================================
# Location (REQUIRED - User must configure)
# ============================================
FEEDER_TZ=UTC
FEEDER_LAT=0.0
FEEDER_LONG=0.0
FEEDER_ALT_M=0
MLAT_SITE_NAME=TAK-ADSB-Feeder

# ============================================
# RTL-SDR Configuration
# ============================================
READSB_DEVICE=0
READSB_GAIN=autogain

# ============================================
# Tailscale VPN (Optional but Recommended)
# Provides secure connection to TAK Server
# ============================================
TAILSCALE_ENABLED=false
TAILSCALE_AUTH_KEY=

# ============================================
# TAK Server (HARDCODED - Always Enabled)
# Priority feed for all aircraft data
# Automatically configured with failover support
# ============================================
TAK_ENABLED=true
TAK_SERVER_HOST_PRIMARY=100.117.34.88
TAK_SERVER_HOST_FALLBACK=104.225.219.254
TAK_SERVER_PORT=30004
TAK_CONNECTION_MODE=auto

# Connection mode options:
# - auto: Automatically select best connection (default)
# - primary: Force primary Tailscale IP
# - fallback: Force fallback public IP
# - monitor: Enable active monitoring and failover (Phase 2)

# ============================================
# Optional Public Aggregators
# Enable these if you want to share with public services
# TAK Server is always fed regardless of these settings
# ============================================

# FlightRadar24
FR24_ENABLED=false
FR24_SHARING_KEY=

# ADS-B Exchange
ADSBX_ENABLED=false
ADSBX_UUID=

# Airplanes.Live
AIRPLANESLIVE_ENABLED=false
AIRPLANESLIVE_UUID=

# RadarBox
RADARBOX_ENABLED=false
RADARBOX_SHARING_KEY=

# PlaneFinder
PLANEFINDER_ENABLED=false
PLANEFINDER_SHARECODE=

# OpenSky Network
OPENSKYNETWORK_ENABLED=false
OPENSKYNETWORK_USERNAME=
OPENSKYNETWORK_SERIAL=

# ============================================
# System Configuration
# Do not modify unless you know what you're doing
# ============================================
ULTRAFEEDER_CONFIG=

# Version tracking for auto-updates (Phase 3)
CONFIG_VERSION=2.1.0
